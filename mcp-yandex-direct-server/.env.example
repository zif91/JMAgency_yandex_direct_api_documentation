# OAuth (Яндекс)
YANDEX_CLIENT_ID=
YANDEX_CLIENT_SECRET=
# Должен совпадать с настройкой в кабинете Яндекс.OAuth
OAUTH_REDIRECT_URI=http://localhost:8787/oauth/callback

# Шифрование токенов (опционально). 32-байтовый ключ base64 для AES-256-GCM
TOKEN_ENCRYPTION_KEY_BASE64=

# Порт для локального OAuth-callback HTTP сервера
OAUTH_CALLBACK_PORT=8787